FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    location    "system";
    object      preciceDict;
}

preciceConfig "precice-config.xml";

participant Fluid2;

modules (FF);

interfaces
{
  Interface1
  {
    mesh              Fluid2_Mesh;
    patches           (leftWall);

    readData
    (
      Alpha
      Velocity
    );

    writeData
    (
    );
  };
};
