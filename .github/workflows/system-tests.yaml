name: System tests

on:
  pull_request:
    types: [labeled]

jobs:
  gather-refs:
    runs-on: ubuntu-latest
    outputs:
      refprecice: ${{ steps.refprecice.outputs.shorthash }}
    steps:
      - id: refprecice
        uses: nmbgeek/github-action-get-latest-commit@main
        with:
          owner: precice
          repo: precice
          branch: develop
      - run: |
          echo ${{ steps.refprecice.outputs.shorthash }}

  trigger-system-tests:
    if: ${{ github.event.label.name == 'trigger-system-tests' }}
    uses: precice/tutorials/.github/workflows/run_testsuite_workflow.yml@develop
    with:
      suites: openfoam_adapter_pr
      build_args: TUTORIALS_REF:develop,PRECICE_REF:${{ needs.gather-refs.refprecice }},OPENFOAM_EXECUTABLE:openfoam2306,OPENFOAM_ADAPTER_REF:${{ github.event.pull_request.head.sha }}
      systests_branch: develop
      loglevel: "DEBUG"
