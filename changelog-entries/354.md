- Updated `preciceDict` parsing for upcoming schema changes. `readData` and `writeData` are now mixed lists of words and/or dictionaries. Dictionaries allow specifying additional options like `solver_name`, `operation`, and `flip-normal` for each field. Backwards compatibility with old schema `wordList` parsing. [#354](https://github.com/precice/openfoam-adapter/pull/354)
